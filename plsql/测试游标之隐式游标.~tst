PL/SQL Developer Test script 3.0
34
-- 测试游标之隐式游标

/*

   游标分类
   
   隐式游标
   
   显示游标（不带参数游标和带参数游标）
      静态游标
      动态游标
          弱类型游标
          强类型游标
          
    游标的使用方式（除隐式游标）
    打开游标  ---->  使用游标  -->关闭游标
    
    游标的几个属性：
          sql%isopen  测试游标是否为打开状态    隐式游标永远为false
          sql%notfound  游标中是否没有找到数据   一般用于loop循环
          sql%rowcount  表示游标的影响的行数
          sql%found  游标中是否有数据  一般用于while循环
          
    如果想从游标中得数据  需要使用fetch into
    
    隐式游标：只要是一条sql语句  就是一个隐式游标  游标名称是sql  
*/
declare 
  -- Local variables here
  i integer;
begin
  -- Test statements here
  
end;
0
0
